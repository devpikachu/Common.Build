{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://git.omni.ms/vintage-story-mods/common-build/raw/branch/main/schema/solution.schema.json",
  "title": "Solution Configuration",
  "description": "Solution-level build configuration",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the overall project",
      "minLength": 1
    },
    "defaultProject": {
      "type": "string",
      "description": "The default project to use when none is specified",
      "minLength": 1,
      "pattern": "^[a-zA-Z\\.]+$"
    }
  },
  "required": [
    "name"
  ]
}
